{% extends 'base.html.twig' %}

{% block title %}ReportCategory{% endblock %}

{% block body %}
    <h1>ReportCategory</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ report_category.id }}</td>
            </tr>
            <tr>
                <th>Label</th>
                <td>{{ report_category.label }}</td>
            </tr>
            <tr>
                <th>Icon</th>
                <td>{{ report_category.icon }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_report_category_index') }}">back to list</a>

    <a href="{{ path('app_report_category_edit', {'id': report_category.id}) }}">edit</a>

    {{ include('report_category/_delete_form.html.twig') }}
{% endblock %}
